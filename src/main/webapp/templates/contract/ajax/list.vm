#foreach ($module in $list)		
	<li aid="${module.id}">
		<a href="/contract/detail/${module.id}" 
		class="ui-shadow ui-btn ui-corner-all ui-btn-inline"
		data-transition="pop" >
			<h2>${module.createtime}</h2>							
			<p>${module.createBy}</p>
			<p>${module.contractName}</p>
		</a>
	</li>
#end